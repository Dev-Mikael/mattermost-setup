# AUTO-GENERATED by scripts/03-bootstrap-flux.sh at deploy time — safe to commit.
# The values here are placeholders; the script overwrites them with real values
# from your .env before committing. Do not edit manually.
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-values
  namespace: flux-system
data:
  DOMAIN: "modumichael.me"
  SERVER_IP: "YOUR_SERVER_PUBLIC_IP"
  SERVER_PRIVATE_IP: "YOUR_SERVER_PRIVATE_IP"
  LETSENCRYPT_EMAIL: "admin@modumichael.me"
  CLUSTER_NAME: "production"
