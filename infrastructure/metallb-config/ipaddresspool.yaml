# AUTO-GENERATED by scripts/03-bootstrap-flux.sh — safe to commit
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: default-pool
  namespace: metallb-system
spec:
  addresses:
    - YOUR_SERVER_PRIVATE_IP/32  # auto-replaced by 03-bootstrap-flux.sh
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: default-l2advert
  namespace: metallb-system
spec:
  ipAddressPools:
    - default-pool
