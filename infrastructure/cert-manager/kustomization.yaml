apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrelease.yaml
# FIX: clusterissuers.yaml is NOT listed here.
# It lives in infrastructure/cert-manager-config/ and is applied
# by the cert-manager-config Flux Kustomization AFTER cert-manager CRDs exist.
