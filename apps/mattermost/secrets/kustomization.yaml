apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# All files here are generated by scripts/04-wait-and-seal.sh
# They are SealedSecret resources — encrypted and safe to commit.
# The Sealed Secrets controller decrypts them into real Secrets at runtime.
resources:
  - sealed-db-credentials.yaml
  - sealed-postgres-auth.yaml
  - sealed-minio-credentials.yaml
